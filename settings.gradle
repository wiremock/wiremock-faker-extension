pluginManagement {
  repositories {
    gradlePluginPortal()
    maven {
      name = "localPluginRepository"
      url = uri("../local-plugin-repository")
    }
  }
}

rootProject.name = 'wiremock-faker-extension'
